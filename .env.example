# 数据库配置（示例）
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_DATABASE=your_database_name

# API配置（支持多种兼容 OpenAI 的服务）
# 选项1：使用本地 Ollama（免费）
API_BASE_URL=http://localhost:11434/v1
API_KEY=not_needed_for_local
DEFAULT_MODEL=llama2

# 选项2：使用 LangChain（需要注册）
# API_BASE_URL=https://api.langchain.com/v1
# API_KEY=your_langchain_api_key
# DEFAULT_MODEL=gpt-3.5-turbo

# 选项3：使用任何 OpenAI 兼容 API
# API_BASE_URL=https://your-api-endpoint.com/v1
# API_KEY=your_api_key_here
# DEFAULT_MODEL=your_model_name

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
LOG_MAX_SIZE=10485760  # 10MB
LOG_BACKUP_COUNT=5

# 缓存配置
CACHE_TTL=3600  # 1小时
CACHE_MAX_SIZE=104857600  # 100MB

# 输出目录
OUTPUT_DIR=output
CACHE_DIR=cache

# 服务端口（可选，默认5000）
# PORT=5000  # 如果5000被占用，系统会自动选择5001-5010中的可用端口

# ==================== Docker 特定配置 ====================
# Docker 环境下的数据库连接配置
# 如果使用 docker-compose.yml 中的 MySQL 服务：
# DB_HOST=db
# DB_PORT=3306

# 如果从 Docker 容器连接宿主机数据库：
# DB_HOST=host.docker.internal
# DB_PORT=3306

# 如果连接外部 Doris 数据库：
# DB_HOST=your-doris-host
# DB_PORT=9030  # Doris MySQL 协议端口